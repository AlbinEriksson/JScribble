package se.albin.jscribble;

/**
 * An empty interface, but it's used to recognize script handler classes when creating a script executor. Add your
 * script functions by creating a <code>public</code> function, and a <code>@ScriptFunction(name="foo")</code>
 * annotation before it.
 */
public interface IScriptHandler
{
}
